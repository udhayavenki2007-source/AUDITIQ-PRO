import { prisma } from "@/lib/prisma"; import { apiError, requireRole } from "@/lib/auth"; import { Role } from "@prisma/client";
export async function GET(request: Request) { try { const user = await requireRole(request, [Role.DEPT_HEAD]); if (!user.departmentId) return Response.json({ error: "Department is required" }, { status: 400 }); const cycle = await prisma.auditCycle.findFirst({ where: { status: "ACTIVE" }, orderBy: { year: "desc" } }); if (!cycle) return Response.json([]); const items = await prisma.checklistItem.findMany({ where: { auditCycleId: cycle.id }, include: { submissions: { where: { departmentId: user.departmentId } } } }); return Response.json(items); } catch (e) { return apiError(e); } }
